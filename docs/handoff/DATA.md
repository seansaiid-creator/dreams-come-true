# 데이터(마케팅)창 핸드오프 — 꿈해몽 행운 (suksuki.com)

> ## 🟢 활성 — 단, 별도 창이 아니라 **운영·전략창(메인 세션)이 이 역할을 겸한다** (2026-09-04~)
> 일의 양이 창 수를 못 따라가므로 데이터·전략을 한 창에서 처리한다. 이 문서는 그 창의 참조 자료다.
> 나중에 분리하면 이 파일 그대로 쓰면 된다.


> 작성 2026-09-04 · 최종 갱신 2026-09-04 19시(데이터창 3회차 추적 후). 이 창은 **GA4 분석·네이버/구글 색인·회복 추적·AdSense 신청 판단·주간 리포트·예약 작업**을 맡는다.
> 코드 배포는 개발창, 문안 작성은 컨텐츠창. 이 창은 **수치로 판단하고 두 창에 지시**한다.

## 0. 먼저 읽을 것 (순서대로)
1. `OPERATIONS.md` **§0 대원칙** · §2-A 데이터 재분석 원칙 · **§3-A 일일 관측 지침** · §4-B
2. `docs/DECISIONS.md` — 특히 **"GA4 당일 데이터로 판단하지 않는다"**
3. `docs/WORKLOG.md` — 오진 2건(9/2·9/3)과 그 교훈
4. 이 파일

## 1. 절대 규칙 (데이터창)
- **당일·전일 판단 금지.** GA4 표준 리포트는 최근 시간대가 0으로 나온다(9/4 실측: 5시간 미처리). 판정은 **완성 데이터(어제까지)**로만
- **급락 의심 시 순서**: ① 실시간 API(`runRealtimeReport`, 지연 없음) ② 사이트 200 + GA 태그 ③ 배포 커밋 시각 대조 ④ 2일 뒤 완성 데이터
- `(not set)` `(data not available)`은 오류가 아니라 **미처리분**
- **예약 작업을 신뢰하지 않는다.** 9/4 정기 실행·즉시 발화 **2회 연속 산출물 없음**(원인 미규명, 로그 조회 도구 없음). 09:00에 보고가 없으면 **즉시 수동 실행**
- **네이버 노출 수치는 이진값만 읽는다.** `naver-recovery-check.mjs:73`은 SERP HTML의 도메인 **문자열 등장 횟수**를 센다 — 표의 13/14/15/28은 순위도 노출수도 아니다. `>0` 여부만 유효(→ 22/50은 유효). 추적 검색어도 페이지 제목 기반 **추정치**라 22/50 자체가 참고치다. 상세는 `docs/DECISIONS.md`
- 숫자를 말할 때 **확인 방법을 함께** 적는다. 확인 못 했으면 "확인하지 못했다"
- 전망·낙관 표현 금지. 회복 기간 예측 금지(네이버 공식 자료 없음)

## 2. 현재 수치 (실측)
| 항목 | 값 | 출처 |
|---|---|---|
| 기준선(전환 전 30일 8/3~9/1) | 사용자 **48.7** · 신규 47.3 · 세션 51.5 · 일 범위 32~67 | `scripts/recovery-baseline.json` |
| 9/2 (전환일) | 41 | 완성 |
| **9/3** | **22 (-55%)** · 신규 21 · 세션 26 | 9/4 18:50 실행. **아직 완결 아님** — 11:19엔 20이었다 |
| 전환 후 2일 평균 | **31.5 (-35%)** | `naver-recovery-check` 9/4 18:50 |
| 9/4 | 10 (처리중) | 판정 제외 |
| 네이버 노출 (상위 50 추적) | **22/50** 노출 · 26 양쪽 미노출 · 2 구도메인만 | 9/4 08:38·11:19·18:50 **3회 동일** |
| **세션 발생 랜딩페이지 수** | 전환 전 7일 **99개** → 9/3 **19개** | GA4 `landingPage` 직접 조회. 스크래핑과 독립 |
| 네이버 세션 일별 | 9/1 47 → 9/2 35 → 9/3 18 | GA4 `sessionSource`×`date` |
| 구글 세션 | 8/26~9/4 **전 구간 0** | 〃 — 전환 전부터 0, 도메인 탓 아님 |
| 미노출 페이지 전환 전 세션 | 656 / 1161 (56%) | 〃 |
| 유입 | 네이버 모바일 97%, 상세 착지 95%, 세션당 1.26p, 90% 스크롤 13~14% | GA4 |
| 사주 | `saju_entry_click` 30일 **0** · `saju_open` 10 · `saju_revisit` 3 | GA4 |

## 3. 원인 — 확정과 배제
**확정**: 도메인 전환 → 네이버가 새 사이트로 재평가 → 페이지별 재색인 진행 중, 그 사이 **구·신 양쪽 미노출 공백**(26/50). 신 도메인 차단 아님(`주차한 차` 등 22개는 노출·AI브리핑 인용).
**배제(실측)**: GA 처리 지연 / 9/3 대량 수정(노출·미노출 같은 커밋) / robots / 리다이렉트(18/18 정상) / 수집 제한(주황 0) / noindex(0) / 308 vs 301(일반 SEO는 동등, 네이버 문서 미확인).
**조치 완료**: 미노출 30 URL 수집 요청 제출(9/3), sitemap 제출, 소유확인.
**남은 것**: 기다리며 매일 실측. 추가 기술 조치 없음.

## 4. 도구
```bash
node scripts/naver-recovery-check.mjs   # 매일: GA4 일별 + 네이버 노출 50건 실측 (약 2분, timeout 400000)
node scripts/recovery-init.mjs          # 기준선·추적대상 재생성 (전환 전 30일)
node scripts/fetch-ga4.mjs 30           # 30일 수집
node scripts/ga/ga-rt.mjs               # 실시간 (지연 없음) ← 급락 의심 시 1순위
node scripts/ga/ga-today.mjs            # 오늘/어제/1주전 시간대별
node scripts/ga/ga-src2.mjs             # 오전 0~12시 유입원 비교
node scripts/ga/ga-dims.mjs             # 맞춤 측정기준 등록 상태 + 인구통계
node scripts/ga/ga-trend.mjs            # 35일 일별 추이
```
GA4 키: `~/.config/dreams-ga4/sa-key.json`(레포 밖). 속성 `528679246`. 측정ID `G-MCNS7P3XVT`.
`content-queue/recovery-log.json` 이력 누적. `scripts/recovery-submit-priority.txt` 제출 목록.

## 5. 예약 작업
| ID | 일정 | 상태 |
|---|---|---|
| `dreams-naver-recovery-daily` | 매일 09:03 | **3회 연속 미발화(9/4 기준). 수동 백업 필수** — `list_scheduled_tasks`에 `lastRunAt` 필드 없음. `SKILL.md`는 정상, **원인 확인 못 함**(로그 조회 수단 없음) |
| `dreams-traffic-verify-0903` | 9/5 11:00 1회 | 9/3 완성치 재확인 |
| `adsense-index-check` | 9/6 10:00 1회 | GSC 색인 20~30+ → AdSense 신청 |
| `dreams-monthly-review` | 매월 1일 10:00 | §2-A 가설표 판정 |

## 6. 계측 현황
- 맞춤 측정기준 7개 등록(9/3): `query` `result_count` `dream` + **`from` `to` `cat` `len_bucket`** — 소급 X, 등록 후 24~48h. `page`는 기본 측정기준으로 대체(등록 불필요)
- **`scroll_25/50/75`**(9/4 배포·실브라우저 검증) — 인라인 모듈 배치 결정용. **`scroll_50 ÷ page_view`, 완성 데이터 7일 후** 판정. 표본 부족하면 그렇다고 보고
- 연령·성별: Google 신호 꺼짐. 켜도 일 50명 규모는 기준점(thresholding)에 걸릴 공산 큼 — 권하지 않음
- 이벤트 계측 자체는 정상(9/4 실브라우저에서 5개 이벤트 + scroll 3개 발화 확인). **클릭이 0인 건 계측 버그가 아니라 위치 문제**

## 7. GA4 설정 — 9/4 감사 및 **조치 완료** (API 재확인)
Admin API 활성화됨. `scripts/ga/ga-admin.mjs`로 조회. 서비스 계정은 Viewer(읽기 전용)라 변경은 운영자 UI.

| 항목 | 9/4 감사 시 | **현재(운영자 조치 후, API 확인)** |
|---|---|---|
| 시간대 | Asia/Seoul | ✅ 그대로 |
| 측정ID | G-MCNS7P3XVT | ✅ 사이트 300곳과 일치 |
| 스트림 기본 URL | 구 `dreams-come-true-ten.vercel.app` | ✅ **`https://suksuki.com`으로 갱신됨** |
| 사이트검색 파라미터 | `q,s,search,query,keyword` | ✅ **`s` 제거됨** → `q,search,query,keyword` |
| 데이터 보관 | 2개월 | ✅ **14개월(FOURTEEN_MONTHS)** — 8월 기준선 보존 |
| Search Console 링크 | 미확인 | ✅ **연결됨** (suksuki.com URL프리픽스 ↔ 웹 스트림) |
| 보고서 컬렉션 게시 | 수명주기·SC 미게시 | ✅ **둘 다 게시됨** — 획득/참여도/유지/SC 화면에서 조회 가능 |

**보고서 게시도 완료**(9/4): 라이브러리에서 `Search Console` + `수명 주기` 컬렉션 모두 "모든 사용자에게 게시됨". 이제 왼쪽 메뉴에 **획득 / 참여도 / 유지 / Search Console**이 보인다. **SC 데이터는 수집 후 48시간 뒤부터**, 구글 유입 자체가 거의 0이라 숫자는 작을 것.
→ **GA4 설정 작업은 전부 종료.** 더 손댈 것 없음.
Admin API에 `searchConsoleLinks` 엔드포인트는 v1alpha/v1beta 모두 404 — **연결 여부는 API로 검증 불가, UI로만 확인**.
`view_search_results`의 `../../etc/passwd`는 봇 프로빙(정적 사이트 + W1-5 화이트리스트라 무해). `s` 제거 후 신규 오인 유입 없는지 다음 주 재확인.

### GA4 UI 경로 (2026-09-04 운영자 화면으로 확정 — 공식 문서와 다름)
- 스트림 URL·향상된 측정: 관리 → **데이터 수집 및 수정 → 데이터 스트림** → 스트림 클릭
- 데이터 보관: 관리 → **데이터 수집 및 수정 → 데이터 보관** (※"데이터 설정" 메뉴는 없음)
- 제품 링크: 관리 사이드바 **하단** → Search Console 링크 / Google 애드센스 링크(승인 후 연결용)

## 8. 대기·판단 사항
- 네이버 **콘텐츠 노출/클릭** 화면(실제 검색어) 캡처 요청 중 — 받으면 `recovery-targets.json` 검색어를 실제 질의로 교체. **9/4 실측으로 이게 왜 필요한지 확정됨**: 노출 0으로 찍힌 페이지 4곳에서 실제 유입 발생(가방을 잃어버리는 꿈 6세션 등)
- **[운영자 컨펌 대기]** `naver-recovery-check.mjs` 노출 표에서 오해를 부르는 숫자를 빼고 `노출/미노출`만 출력하도록 1줄 수정 — 판정에 쓰는 값은 어차피 이진값이라 결과는 안 바뀐다. **미이행**(제안만 한 상태, 9/4)
- 9/6 GSC 색인 수 → AdSense 신청 여부. 네이버 트래픽과 무관(AdSense는 구글 색인·콘텐츠 품질·정책, 최소 트래픽 요건 없음 — 구글 공식)
- 유료화·카카오톡 일일 발송: 전제(사업자등록·통신판매업) 충족. **일 500명 전 보류** — 현 트래픽 유료전환 1% = 월 4.5만 원. 알림톡 7~14원/친구톡 15원(2026). 백엔드·DB·계정 없음, 생년월일 서버 저장이 "서버 미저장" 약속과 충돌
- 주간 이메일 리포트(월 09:30) 정상. 월간 리뷰가 §2-A 가설 7개 판정

## 기록 규칙 (모든 창 공통)
결론은 대화에만 남기지 않는다. **세션 간에 대화는 공유되지 않으며, 레포 파일과 메모리만이 통로다.**
- 사건·문제·교훈 → `docs/WORKLOG.md` (증상→원인→처리→교훈)
- 하기로/안 하기로 한 것 → `docs/DECISIONS.md` (이유·날짜)
- 운영 기준·전략 변경 → `OPERATIONS.md`
- **이 창의 현재 상태·대기 작업 → 이 핸드오프 파일을 직접 갱신**
- 다른 창 핸드오프는 **읽기만**. 지시를 남길 때만 그 창의 "대기 작업" 절에 추가
- 수치는 확인 방법과 함께. 추측은 "추측"이라 명시. 결론이 날 때마다 즉시 기록
전문: `docs/handoff/RECORDING_RULE.md`

## 창 경계
| 창 | 소관 |
|---|---|
| 전략 | 무엇을·왜·언제 (방향 결정) — `docs/handoff/STRATEGY.md` |
| 데이터 | 수치 측정·해석·색인 추적 |
| 컨텐츠 | 문안 작성·검수·발행 큐 |
| 개발 | 구현·배포·계측 |
내 소관이 아닌 일은 **"○○창에 넘긴다"고 말하고 하지 않는다.** 방향이 필요한 판단은 전략창으로.

## 9. 새 창 첫 메시지 (붙여넣기)
```
데이터창이다. docs/handoff/DATA.md를 읽고 시작해. OPERATIONS.md §0 대원칙과 "당일 데이터 판단 금지" 준수.
지금 할 일은 [여기에 지시]. 수치는 확인 방법과 함께 말해.
```
