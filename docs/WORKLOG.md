# 꿈해몽 행운 — 작업 이력 · 현재 상황

> **역할 분담**: 이 파일은 **한 일과 그때 터진 문제**를 적는다.
> `DECISIONS.md` = **안 하기로 한 것**(새 세션이 다시 제안하지 않게) · `../OPERATIONS.md` = **운영 규칙·KPI·컨펌 게이트** · `SECURITY_REVIEW.md` = **보안 검토와 조치 현황**
> 작업할 때마다 갱신한다.

---

## 📊 한눈 요약 (2026-09-04 갱신)

**한 줄 상태(09-04)**: 도메인 전환(9/2) 여파로 **네이버 재색인 공백** 진행 중 — 9/3 사용자 **22명(기준선 48.7 대비 -55%)**. 기술적 원인은 전부 배제됐고 **기다리며 매일 실측하는 국면**. 병행해서 계측·문안·문서 체계를 정비했다.

**서비스 기본값**
- 주소: https://suksuki.com (구 `dreams-come-true-ten.vercel.app` → 308 리다이렉트)
- 콘텐츠: 꿈풀이 **142편** + 꿈사주(`/saju.html`)
- 규모: **일 사용자 약 50명**, 세션당 1.26페이지, 재방문율 ~0.2%
- 유입: **네이버 97%** / 구글 0% (색인 회복 진행 중) / 모바일 94%
- 비용: **0원** (도메인 기보유, Actions는 public repo라 무제한 무료)
- 수익: 현재 **0원** (AdFit 제거함, AdSense 미승인)

**🟡 09-03 확인: "사용자 급락" 신고 → 급락 아님, 측정 문제였음**
운영자가 대규모 수정 다음 날 사용자 급감을 우려. 조사 결과 **트래픽 손실 없음**.
- 9/2 = 41명, 14일 범위(36~62, 평균 48.4) 안. 8/28에도 39명, 8/20에도 36명
- 당일(9/3) 2명은 **조회 시각이 KST 07:46** 이라 하루가 시작도 안 한 것 + GA 처리 지연
- 실시간 API로 유입 지속 확인(최근 30분 6명), 네이버 경로 end-to-end 정상, 콘솔 오류 0
- **진짜 변한 것**: 유입원 추적. `(not set)`이 0 → 25(36%)로 튀고 네이버 referral 47 → 32.
  그러나 **총 세션은 49 → 69로 증가** — 사용자가 사라진 게 아니라 리다이렉트 체인
  (네이버 → 구도메인 → 308 → suksuki.com)에서 referrer가 유실돼 미분류된 것
- 조치: 네이버 서치어드바이저 소유확인 복구(새 도메인용 인증코드가 새로 발급돼 있었음) →
  사이트맵 제출 + 홈 수집 요청 완료. 네이버가 새 도메인을 직접 색인하면 체인이 사라져 해소됨
- **판단 기준**: 완료된 날 기준으로만 볼 것. 40명 아래 이틀 연속이면 그때 재조사

**✅ 09-03: AdSense 계정 정리 완료** — 콘솔의 반려 사이트 3개 전부 삭제, 미사용 서비스 2곳
광고·ads.txt 제거·noindex 처리. `suksuki.com/ads.txt`는 정상 서빙 중이라 **신청 준비는 끝난 상태**.
남은 조건은 구글 색인 하나뿐이다.

**🔥 지금 기다리는 것 — 구글 색인**
구글이 사이트를 **크롤링하고도 색인을 거부**하던 상태였다(`*.vercel.app` 공유 서브도메인이 원인).
도메인 이전 직후 **홈이 색인됨** + sitemap 145개 인식 → 원인 진단이 사실로 확인됨.
개별 페이지 색인이 쌓이는 중이며 **이게 AdSense 신청의 유일한 남은 조건**이다.
→ **09-06 알림 예약됨**(예약작업 `adsense-index-check`). GSC 색인 20~30개 넘으면 신청.

**⏰ 사용자 차례인 일**
- ~~**초안 8편 검수**~~ → **완료 (09-04 실측)**. `content-queue/queue.json` 13건 **전부 `approved:true`**, 발행 완료 4 · **미발행 9**. 월수금 04:10 워크플로가 순차 발행(3주치). 운영자 차례 아님
- **09-06 구글 색인 확인** → AdSense 신청 판단
- (선택) GA4 Google 신호 켜기 — 현재 연령·성별 데이터 없음

**⬜ 데이터 대기 중인 판정 (2~4주 후)**
- **행운번호 기능** — `cta_lucky_click`/조회 ≥5%면 유지·강화, ~0이면 축소. 지금까지 사용 0이었지만 **원인은 끊긴 퍼널이었다**(오늘 고침)
- **꿈사주 차별점 전략** — `saju_dream_picked`·`saju_entry_click`·재방문율로 판정
- **본문 회유 링크 효과** — 세션당 페이지 1.26 → 1.5 목표

---

## ✅ 오늘 한 일 (2026-09-02) — 커밋 순

### 1) 진단 (8개 역할 분석)
기획자·기획검토자·개발자·개발검토자·UI/UX·사업담당 각 3라운드 논의 → PO1·PO2 합의 보고서.
[최종 보고서 아티팩트](https://claude.ai/code/artifact/b913ad6f-e5da-403c-9945-56464cb5476d)

### 2) D0 배포 (`c816d09`) — 계측 + 색인 회복
- GA4 커스텀 이벤트 7종 심음 (그전까지 `memimo_click` 하나뿐이었다)
- sitemap 104 → **144 URL**, lastmod 실제 커밋일로
- **canonical이 404를 가리키던 19페이지** 수리 (뱀꿈·물꿈 등 코어 포함)
- `vercel.json` 신설 — 유령 URL **57종 301**
- 404 페이지 신설, `index.html`에 누락돼 있던 `</body></html>` 복구

### 3) W1-12 배포 (`f906b92`) — 죽어 있던 퍼널 수리
- **139개 페이지의 "행운번호 받기" 버튼이 아무 동작 없는 빈 홈에 착지**하고 있었다 (처리 코드 0줄).
  `?kw=&cat=` 규격 + 레거시 `?dream=` 호환 핸들러 구현. XSS 방어(textContent 전용·화이트리스트) 포함
- 무관한 "택시 꿈" 문구가 복붙된 6파일 수리, `dream-tooth` 회유 섹션 이중 삽입 제거(광고 7→4)
- 깨진 상대링크 **전량 0건** 달성, guide 사전 139/139 등재
- og:image 144페이지, `user-scalable=no` 제거(WCAG)

### 4) AdFit 전면 제거 (`674043b`)
누적 수익 300원 미만 확인 → 제거. 미충전 빈 광고 박스가 AdSense 저가치 재판정 리스크였다.

### 5) W2-0 도메인 이전 (`5b9a6ed`, `ba0f69d`)
- 전 URL을 `suksuki.com`으로, 구 도메인 → 308 리다이렉트
- **결과: 구글이 홈을 색인함** (구 도메인에선 "크롤링됨-색인 생성되지 않음"이었다)

### 6) CTA 착지 정확도 (`69556b2`) — 61% → 0%
CTA 138종 중 **86개(61%)가 카테고리 일반론으로 착지**하고 있었다. 상위 트래픽 페이지 전부 해당.
→ `dreams-db.js`(139페이지 실제 해몽 추출, 127KB) 신설. CTA에 `&s=<slug>` 추가, 핸들러가 동적 로드.
홈 직접 방문 시엔 로드하지 않아 성능 영향 없음.

### 7) 발행 파이프라인 완전 자동화 (`8b21377`)
발행 → `dreams-db` 재생성 → **무결성 검증**(`verify-publish.mjs`) → 커밋 → 배포까지 무인.
검증 항목: DB·지정표 등재, 본문 길이, 길흉 등급, CTA 슬러그, **AdFit 재삽입**, **구 도메인 잔존**.
실패 시 워크플로가 멈춰 잘못된 상태가 배포되지 않는다.

### 8) 회유 동선 + 공유 수리 (`1cba62c`)
- 본문 8,000자 구간에 내부 링크가 **0개**였다(5개가 전부 97~98% 지점). 28% 지점에 관련 해몽 3링크 삽입 — 사용자 회유이자 **구글이 139페이지를 발견하는 통로**
- 카카오 SDK가 상세 페이지에 없어 **"카카오톡" 버튼이 링크복사로만 동작**했다. SDK 로드 + feed 공유 구현, 3단 폴백

### 9) 문서 정비
- `about.html` — "20+ 해몽" → **실제 142편**(심사관이 과소평가할 표기였다), 콘텐츠 제작 방침 섹션 신설
- `privacy.html` — **실제 쓰고 있던 GA4가 문서에 없었다**. GA4·Kakao SDK·Vercel 명시, 이용자 권리 조항
- `terms.html` — **로또를 다루면서 연령 고지가 없었다**. 제4조 신설(만 19세, 도박문제 1336 안내)

### 10) 로또 표기 정리 (`7896756`)
"행운번호"라 부르면서 title·keywords엔 "로또"를 박아둔 상태 → 표기와 실제 불일치 해소.
근거: 로또 관련 페이지가 전체 조회의 **0.7%(45/6,069)**. 유입은 해몽에서 오지 로또에서 오지 않는다.

### 11) 운영 OS 구축
- `OPERATIONS.md` — KPI·확정 규칙·**컨펌 필수 항목**(배포/비용/외부서비스/콘텐츠 승인/도메인/광고)
- `/ops` 스킬 — 주간 사이클(수집→분석→제안→컨펌→반영)
- 자동화 5종: 매일 오늘의 꿈 회전 / 월수금 발행 / 월 09:00 헬스체크 / **월 09:30 비교 리포트 메일** / 월 1일 월간 전략 리뷰(예약작업)
- **GA4 Data API 연결** — 이제 CSV 없이 직접 데이터를 당긴다

### 12) 꿈사주 신규 출시 (`bb32411` ~ `39f61a7`)
- **만세력 코어** — 절기·한국 표준시 이력(1954~61 UTC+8:30, 서머타임 12구간)·야자시 처리. 전량 브라우저 계산
- **음력 입력** — 삭 계산 + 무중치윤법. KASI 41건 대조 전항목 일치
- **문안 63개로 400조합** — 일간 10 + 오늘의 흐름 5 + **꿈×흐름 다리 40** + 오행 5
- **차별점 인지 전략** — '꿈사주' 명명, 꿈 선택기 신설, CTA 개인화, 근거 라벨, 공유 문구
- 검증 **319건 전부 통과** (`node saju/verify.js`)

### 13) 콘텐츠
- 신규 초안 13편 작성 → **5편 승인(3편 발행 완료)**, **8편 검수 대기**
- 미커버 고검색량 주제 발굴: 머리카락 빠짐·알몸·목소리 안 나옴·뛰는데 안 나아감·브레이크·높은 곳·반려동물·아이 상실

---

## ✅ 09-04 한 일 (4개 창 합산)

**이 날부터 창을 나눠 작업했다.** 아래는 운영·전략창(메인) 기준이며, 개발·컨텐츠·데이터창이 남긴 항목은 「터진 문제와 처리」 절에 각각 있다.

### 1) 네이버 회복 추적 (데이터)
- 9/3 완성치 **22명 (-55%)**. 전환 후 완성 2일 평균 30.5명 → 이후 갱신은 `content-queue/recovery-log.json`
- 노출 22/50 (미노출 26, 구도메인만 2). 3회 측정(08:38·11:19·18:53) 동안 **변화 없음**
- ~~독립 지표 발견: 랜딩페이지 폭 99개 → 19개~~ → **09-06 철회.** 7일치와 1일치를 비교한 무효 수치. 하루끼리 재측정하면 페이지/사용자 비율이 전 기간 동일하다
- **원인 배제 완료**: GA 처리지연 / 9/3 대량 수정 / robots / 리다이렉트(18/18) / 수집제한 / noindex / 308vs301

### 2) GA4 설정 감사·조치 5건 (`16dedc8`~`699d448`)
Admin API를 켜서 설정을 직접 조회 → 운영자가 당일 전부 조치, API로 재확인.
| 항목 | 전 | 후 |
|---|---|---|
| 스트림 기본 URL | 구 vercel.app | `https://suksuki.com` |
| 사이트검색 파라미터 | `q,s,search,query,keyword` | **`s` 제거** (CTA 착지 `?s=`가 검색으로 오인되던 것) |
| 데이터 보관 | 2개월 | **14개월** (8월 기준선 보존) |
| Search Console 링크 | 미연결 | 연결 |
| 보고서 컬렉션 | SC·수명주기 미게시 | 둘 다 게시 |
- 시간대 `Asia/Seoul` 확인 → 그동안의 시간대별 비교 분석 전부 유효
- **수집 자체는 도메인 전환과 무관하게 정상**이었다(실브라우저에서 `g/collect` 패킷 직접 포착)

### 3) 스크롤 깊이 계측 `scroll_25/50/75` (`35f4fb3`, `a37759d`)
- 143 dream 페이지 + 템플릿. 인라인 꿈사주 모듈의 배치 지점(본문 51%) 도달률을 재기 위함
- 판정 기준: **`scroll_50 ÷ page_view`, 완성 데이터 7일 이상**
- 배포 직후 실브라우저 검증에서 **0건** → rAF가 hidden 탭에서 멈춰 스로틀이 고정되는 버그. setTimeout으로 교체 후 재검증 통과

### 4) 사업자등록번호 표기 (`4f08ad6`)
- 150개 전 페이지 푸터 + about·privacy·terms + 템플릿. 상호 에스이컴퍼니 / 662-30-01954
- 근거: memimo(any-scripter) 법적 문서에서 사업자등록·통신판매업 신고 완료 확인
- 대표자·주소·전화는 **의도적으로 제외**(현재 판매 행위 없음, 운영자 보안 민감도). 통신판매업 신고번호는 유료화 시점에 추가
- ⚠️ 정정: 이 조치가 AdSense 심사에 도움이 된다고 말했으나 **근거 없는 추론이었다**. 낮은 비용에 확실한 소득(운영 주체 검증 가능)이 있어 한 것이지 심사 효과는 확인되지 않았다

### 5) 꿈사주 문안 2차 초안 50블록 (`bb2621b`)
- `docs/drafts/saju-content-v2-draft.md` — 꿈 처방 40 + 오늘의 흐름 10. **미반영, 검수 대기**
- 80블록 전부가 아니라 **차별점(꿈 처방)과 재방문 동기(오늘의 흐름)로 좁혔다.** 일간 30블록은 위치 수정 후 데이터 보고 결정
- 방침: "길게"가 아니라 "구체적으로" — 블록마다 오늘 할 행동 하나, 사주 용어 0
- 1차본에서 글자 수를 손으로 세어 **49/50 오기** → `scripts/measure-draft.py` 신설(실측·범위·용어 검사)

### 6) 전략 검토 6건 (운영자 요청) — 결론은 `docs/handoff/STRATEGY.md` §3
- 꿈×사주 결합: **이동 요구 설계는 실패 확정**(30일 0건) → 꿈 페이지 안에서 완결하는 인라인 설계로. AdSense 승인 후
- 유료화·카카오톡 일일 발송: 전제(사업자등록) 충족하나 **일 500명 전 보류** — 현 트래픽 유료전환 1%면 월 4.5만 원, 구축비 회수 불가
- 타로·이름궁합: 둘 다 단일 HTML, **꿈과 연결점 없어 해자에 기여 안 함.** 우선순위 아님(타로는 Gemini 키가 비어 AI 풀이가 죽어 있음)

### 7) 창 분리 + 문서 체계 (`e04f566`, `cb6d4d5`, `6356246`)
- `docs/handoff/{DEV,DATA,CONTENT,STRATEGY}.md` 신설 — 담당·규칙·현재 상태·대기 작업·첫 메시지
- `docs/handoff/RECORDING_RULE.md` — 4창 공통 기록 규칙(원본). 각 핸드오프에 절로 삽입
- **핵심 제약 명시**: 세션 간 대화는 공유되지 않는다. 레포 파일과 메모리만이 통로다
- **한 창도 안전하지 않다** — 이 세션도 압축(요약)을 겪었다. 기록은 창 분리 여부와 무관하게 필요하다
- 운영 형태: 일의 양이 4창을 정당화하지 못해 **컨텐츠만 분리, 데이터·전략은 메인이 겸함, 개발은 휴면(AdSense 승인 후)**. 핸드오프 상단에 🟢활성/🟡휴면 표시
- GA 조회 스크립트 8종을 세션 스크래치 → `scripts/ga/`로 이관

### 이날 내가 틀렸던 것 (정정 이력)
- 문안 글자 수 손계산 **49/50 오기** → 실측 스크립트로 대체
- GA4 데이터 보관 "소급 안 됨" → **적용됨**(구글 공식)
- GA4 메뉴 경로 **두 번 틀림** — 공식 문서와 실제 UI가 달랐다. 운영자 화면 캡처를 먼저 받는 편이 빠르다
- 사업자번호 표기의 AdSense 효과를 근거 없이 주장
- 9/3 사용자 수를 **20명(-59%)**으로 기록 → 데이터가 더 처리되어 **22명(-55%)**로 확정. 당일 데이터는 며칠 뒤에도 움직인다

## 📉 09-06 분석 — 바닥 횡보, 무효 지표 1건 철회

### 유입 (metrics.mjs 기준, 9/5는 미확정)
| 일자 | 사용자 | 네이버 세션 | 비고 |
|---|---|---|---|
| 09-01(화) | 48 | 44 | |
| 09-02(수) | 41 | 33 | 전환일 |
| 09-03(목) | 22 | 18 | |
| 09-04(금) | **21 확정** | 17 | |
| 09-05(토) | **19 잠정** | 19 | 어제 관측 13 → 오늘 19로 상승 중 |
- 같은 요일 비교: 08-29(토) 62 → 09-05(토) 19 = **-69%**
- 네이버 세션 18→17→19. **표본이 작아 반등인지 잡음인지 구분 불가**
- **9/5를 어제 수치(13)로 판단했다면 또 틀렸다.** `metrics.mjs`가 "직전 실행과 다름"으로 차단함

### 노출 (스크래핑, 보조 지표)
09-03 19 · 09-04 22 · 09-05 23 · **09-06 23 — 이틀째 정체**
구 도메인만 노출: 6 → 2 → 0 → **0**. 구 도메인은 완전 소멸했고 신 도메인 순위만 남았다.

### 스크롤 (dream 조회 69 누적, 표본 부족)
25% 30건(43%) · 50% 21건(30%) · 75% 12건(17%). 어제(51조회)와 비율 유사.
**25% 이전에서 가장 많이 빠지고, 넘긴 사람의 70%가 중반까지 간다.** 300조회까지 약 6일.

### ⚠️ 철회 — "랜딩페이지 폭 붕괴 99→19"
09-04에 데이터창이 **"가장 뚜렷한 실측"**으로 기록했으나 **무효 비교였다**(7일치 99개 vs 1일치 19개).
하루끼리 재측정하고 사용자 수로 정규화한 결과:
| 일자 | 사용자 | 랜딩페이지 | 페이지/사용자 |
|---|---|---|---|
| 08-28(금) | 39 | 29 | 0.74 |
| 08-29(토) | 62 | 38 | 0.61 |
| 09-02(수) | 41 | 30 | 0.73 |
| 09-03(목) | 22 | 19 | 0.86 |
| 09-04(금) | 21 | 16 | 0.76 |
| 09-05(토) | 19 | 15 | 0.79 |
**페이지/사용자가 전 기간 0.61~0.86으로 동일하다.** 폭 축소는 사용자 감소에 비례할 뿐이며
"롱테일이 죽고 있다"는 해석은 근거가 없다. `DECISIONS.md`·`DATA.md`·`WORKLOG` 4곳 정정.
→ **재발 방지**: `OPERATIONS.md` §0 적용 규칙에 "비교하는 두 값의 기간 길이가 같은지 확인, 다르면 정규화" 추가.

### 결론
**회복 신호 없음 · 추가 악화 없음 · 바닥 횡보.** 기술 원인은 09-05에 전면 배제됐으므로 손댈 곳이 없다.
09-10 판정 기준 유효(서치어드바이저 노출수 300+ / 150 이하).

## 🔎 09-05 네이버 진단 — 기술 원인 전면 배제, 순위 회복 대기로 확정

운영자가 서치어드바이저 3개 화면(콘텐츠 노출/클릭, 수집 현황, URL 검사)을 제공. **처음으로 네이버 측 실측을 확보**했다.

### 확인된 사실
| 항목 | 실측 | 해석 |
|---|---|---|
| **크롤링** | 최근 이틀 **123 → 75페이지 수집**, 수집 제한 전 구간 0 | 로봇이 활발히 온다. 막힌 곳 없음 |
| **URL 검사**(`dream-bag-lost.html`) | 접속 200 · robots 허용 · **로봇 메타 태그 "검색 결과에 노출할 수 있음"** · 제목·설명·OG 전부 ✓ | **기술적 차단 요인 전무** |
| **검색 노출** | TOP30 합계 노출 **104** · 클릭 **3** · CTR **2.9%** | 노출되면 클릭은 된다. **노출량 자체가 붕괴** |
| **1위 유입원** | 「가방을 잃어버리는 꿈」(전환 전 147세션) **TOP30에 없음**(노출 0). 휴대폰·돈받는 꿈도 없음 | 상위 3개만 완전 소실 |
| 다운로드 시간 | 구 도메인 150~250ms → 신 도메인 450~700ms | 대량 크롤링 중이라 그럴 수 있음(추정). 관찰 대상 |

### ⚠️ 내 측정 방법의 오류 — "미노출 27개"는 과대평가였다
`naver-recovery-check.mjs`는 **m.search.naver.com 검색 1페이지만** 긁는다. 2페이지 이하로 밀린 페이지를 전부 "미노출"로 셌다.
운영자 데이터로 반증됨:
| 페이지 | 내 판정 | 실제 노출 |
|---|---|---|
| 엘리베이터 타는 꿈 | ❌ 미노출 | **7건** |
| 신발 잃어버린꿈 | ❌ 미노출 | 1건 |
| 누군가 도와주는 꿈 | ❌ 미노출 | 1건 |
→ **"색인 안 됨"과 "색인됐으나 순위 낮음"을 구분하지 못한다.** 대응이 완전히 다른 두 상태를 하나로 뭉갰다.

### 결론 — 진단 확정
1. 크롤링 정상 · 색인 진행 중 · 기술 차단 없음 · CTR 정상
2. 남은 문제는 **순위**다. 새 도메인이 신뢰도를 다시 쌓는 과정이며 **기술적으로 더 할 것이 없다**
3. 재제출은 효과가 제한적으로 보임(크롤링이 이미 활발하므로) — 추정
4. **하지 말 것**: 도메인 되돌리기, 본문 대규모 수정

### 판정 지표 변경 (09-05)
내 스크래핑이 부정확하므로 **서치어드바이저 「콘텐츠 노출/클릭」의 노출수**를 1차 지표로 쓴다.
- 현재(09-05): TOP30 노출 **104**
- **09-10 기준**: 노출 300+ → 회복 중 / 150 이하 → 정체, 네이버 고객센터 문의 검토
- 스크래핑 수치(`naver-recovery-check.mjs`)는 **보조 지표**로 강등. 값 자체보다 추세만 본다

## 🔴 오늘 터진 문제와 처리

### [해결] 4개 창이 **한 워크트리를 공유**한다 — 다른 창 커밋이 내 미커밋 변경을 삼켰다 (09-04 19:10, 개발창)
- **증상**: 개발창이 `docs/WORKLOG.md`를 편집해 두고 `OPERATIONS.md`·`DEV.md`와 함께 커밋하려 했는데, 커밋 결과물(`cc97947`)에 **WORKLOG가 들어가지 않았다.** 워킹 트리에도 변경이 안 보였다(내용은 파일에 그대로 있는데 `git status`가 깨끗).
- **원인**: 같은 시각 **컨텐츠창이 `085e86d`을 커밋하면서 `docs/WORKLOG.md`를 경로 지정으로 add** 했고, 그 시점 인덱스에 올라간 파일 내용에 **개발창의 미커밋 편집이 통째로 포함**돼 함께 커밋됐다. 4개 창(전략·데이터·컨텐츠·개발)이 **같은 디렉터리·같은 브랜치**에서 동시에 작업한다는 사실이 어느 문서에도 적혀 있지 않았다.
- **확인 방법**: `git show 085e86d -- docs/WORKLOG.md` — 개발창이 쓴 「WORKLOG의 큐 상태 기술이 실물과 불일치」 항목이 컨텐츠창 커밋 안에 있다. 커밋 시각 19:10:01, 개발창 커밋 19:10 직후.
- **파급**: **내용 유실은 없다**(HEAD에 양쪽 기록 모두 존재). 깨진 것은 커밋 경계와 이력 — 커밋 메시지가 실제 변경을 설명하지 못한다. 타이밍이 조금 달랐다면 **한쪽이 상대의 편집 중 파일을 덮어써 유실**될 수 있었다.
- **함께 발견된 2차 문제**: 전략창이 `docs/handoff/RECORDING_RULE.md`(원본)와 4개 핸드오프 말미에 기록 규칙을 이미 넣어둔 상태에서, 개발창이 그를 모르고 `OPERATIONS.md §0-A`에 **같은 규칙 전문을 3중으로 복제**했다. 원본 참조 한 줄로 축소함(같은 커밋).
- **처리**: `DECISIONS.md`에 공유 워크트리 규칙 신설(편집→즉시 커밋, 커밋 직전 `git log --oneline -1` 대조, `git diff --cached --stat`으로 내 변경만인지 확인). `OPERATIONS.md §0-A`는 원본 포인터로 축소.
- **교훈**: **`git add -A` 금지만으로는 부족하다.** 경로를 지정해도 *같은 파일*을 두 창이 동시에 편집하면 섞인다. 공유 문서(`WORKLOG.md`·`DECISIONS.md`)는 **편집한 즉시 커밋하고 오래 들고 있지 않는다.** 그리고 **커밋 전에 다른 창이 방금 무엇을 넣었는지부터 본다** — 규칙·문서를 신설하기 전에도 마찬가지다(3중 복제가 그렇게 났다).

### [해결] WORKLOG의 큐 상태 기술이 실물과 불일치 — "8편 검수 대기"는 이미 해소돼 있었다 (09-04, 개발창)
- **증상**: 개발창 착수 전 상태 점검에서, WORKLOG 두 곳(요약 「⏰ 사용자 차례인 일」·「미해결·보류」)이 **"초안 8편 검수 — 사용자 차례"**로 적혀 있었다.
- **실측**: `content-queue/queue.json` 파싱 결과 **13건 전부 `approved:true`**. 발행 완료 4(washing-machine-overflow·kakao-message-unread·wifi-keeps-disconnecting·subway-gate-not-opening) · **미발행 9**. 즉 운영자 차례인 일이 아니라 월수금 04:10 워크플로의 자동 발행 대기 상태였다.
- **원인**: 운영자가 큐를 승인한 시점에 문서를 갱신하지 않았다. 큐 상태는 **파일에서 계산되는 값인데 문서에 손으로 적어 두어** 실물과 어긋났다(같은 유형의 과거 사고: 문안 글자 수를 손으로 세어 49/50 오기 → `measure-draft.py`로 실측 전환).
- **파급**: 실제 피해는 없었으나, 새 창이 이 문서를 근거로 **운영자에게 이미 끝난 일을 다시 요청**할 수 있었다.
- **처리**: WORKLOG 두 곳을 취소선 + 실측값으로 정정(이 커밋). 발행 무결성은 `node scripts/verify-publish.mjs` 통과(페이지 143 / DB 143)로 별도 확인.
- **교훈**: **파일에서 세면 되는 수치를 문서에 고정 문장으로 적지 않는다.** 적어야 하면 확인 명령을 함께 적어 다음 창이 재실측할 수 있게 한다. 새 창은 문서의 "대기 중" 기술을 그대로 믿지 말고 착수 전에 실물을 확인한다.

### [해결] AdFit이 되살아나 라이브에 노출됨 — 내가 만든 사고
**증상**: 09-02 AdFit을 제거했는데(`674043b`), 이후 UI/UX 재점검에서 **141개 파일에 다시 살아있는 것**이 발견됐다. 라이브에도 나가 있었다.
**원인**: `docs:` 커밋(`25b14c8`)을 하면서 `git add -A`로 워킹 트리 전체를 담았고, 그 안에 옛 버전 파일이 섞여 있었다. **커밋 전 `git diff --stat`을 확인하지 않은 내 잘못.**
**처리**: 재제거 후 배포 + `health-check.mjs`에 **AdFit 재삽입 감시**와 **구 도메인 잔존 감시** 추가 → 재발 시 월요일 헬스체크가 잡는다. 발행 워크플로의 `verify-publish.mjs`에도 같은 검사를 넣어 자동 발행 경로로는 재발 불가.
**교훈**: 커밋 전 **변경 파일 수를 반드시 눈으로 확인한다.**

### [해결] 구글이 사이트를 크롤링하고도 색인하지 않음
**증상**: 92일간 구글 유입 **0회**. GSC에서 홈이 "크롤링됨 – 현재 색인이 생성되지 않음", 개별 페이지는 "아직 알려지지 않은 URL". sitemap 마지막 읽기가 **5개월 전(4/10)**, 발견 페이지 35개.
**1차 가설(부분적으로만 맞음)**: canonical 404 19건 때문에 구글이 깨진 사이트로 판단 → 수리 후 sitemap 재제출. **오늘 날짜로 재읽기 성공, 145개 인식**. 하지만 색인은 여전히 거부.
**2차 가설(적중)**: `*.vercel.app`은 누구나 쓰는 **무료 공유 서브도메인**이라 구글이 신뢰도를 낮게 매긴다. 네이버는 이런 구분을 덜 해서 네이버에서만 잘 잡히던 패턴이 정확히 설명된다.
**검증**: 도메인 이전 직후 `suksuki.com` 홈이 **"URL이 Google에 등록되어 있음 / 색인 생성됨"** 으로 바뀜. 같은 콘텐츠인데 도메인만 달라졌다. **가설 확정.**
**남은 것**: 개별 페이지 색인이 쌓이는 중. 09-06 확인 예정.

### [해결] CTA 착지의 61%가 엉뚱한 해몽
**증상**: 사용자가 8,000자짜리 "가방 잃어버리는 꿈"을 읽고 버튼을 누르면 *"무언가를 잃어버리는 꿈은…"* 같은 일반론이 나왔다. **14개 분실 꿈이 전부 같은 문장·같은 등급**.
**원인**: 홈의 해몽 DB가 48개뿐인데 페이지는 139개. 매칭 실패 시 카테고리 폴백으로 떨어졌다.
**처리**: 각 페이지의 실제 해몽 본문·조언을 추출해 `dreams-db.js` 생성, CTA에 슬러그를 실어 정확히 조회. 길흉 등급은 사이트가 이미 손으로 정한 값을 우선 재사용.
**주의**: 신규 발행 시 재생성이 필요한데 **수동 단계를 남기면 언젠가 빠뜨린다** → 발행 워크플로에 자동 실행으로 넣음.

### [해결] 음력 변환 버그 2건 — KASI 대조가 아니었으면 놓쳤다
- **연도 귀속 오류**: `2010-12-22` → 음력 2009년으로 나옴(월·일은 맞음). 원인은 마지막 동지월(11월)의 연도를 `sy-1`로 일괄 계산한 것. 실제로는 그 달이 `sy`에 속한다 → 구성 시점에 연도를 명시적으로 부여하도록 수정
- **윤달 번호 규칙**: 2020년 **윤4월**을 윤5월로 표기. 윤달은 **직전 달 번호를 물려받는데** 다음 번호를 쓰고 있었다 → 수정
- 수정 후 KASI 41건 전항목 일치

### [해결] 사주 월주가 KASI와 3건 불일치 — 알고 보니 개념 차이
**증상**: KASI 대조에서 월주 3건이 달랐다.
**판정**: KASI가 주는 `LUNC_WLGN`(월건)은 **음력 달의 간지**이고, 사주 월주는 **절기 기준**이라 원래 다른 값이다. 절기 시각으로 우리 값이 맞음을 확인(1990-03-05 경칩 3/6 05:15 → 인월 등).
**처리**: 대조 항목에서 월건 제외하고 그 근거를 코드 주석에 남김. **불일치를 무조건 버그로 보지 않는다.**

### [해결] GitHub Secrets 등록 사고
- `GA4_SA_KEY_B64`에 **13자짜리 엉뚱한 값**이 들어가 있었다(정상은 3,224자). 진단 워크플로로 길이·형식만 출력해 원인 특정 → 재등록
- `MAIL_APP_PASSWORD`에 Google 계정 비밀번호를 넣어 `534 5.7.9` 오류 → 앱 전용 비밀번호 발급으로 해결
- macOS `base64`가 줄바꿈을 넣어 리눅스 디코더가 거부 → 워크플로에서 개행 제거 + base64/원본 JSON 양쪽 허용하도록 보강

### [해결] GCP 조직 정책이 서비스 계정 키 생성을 차단
`seansaiid-org`에 `iam.disableServiceAccountKeyCreation`이 기본 적용돼 있었다.
→ 해당 프로젝트에만 **재정의(사용 안 함)** 로 예외 처리. 다른 프로젝트 보안은 유지.
(참고: `koreafestival` 프로젝트에 memimo용 Gemini 키가 살아 있으므로 건드리지 말 것)

### [해결] 사용자가 쓴 꿈 내용이 Google Analytics로 전송되고 있었다 — 09-02 보안 검토
**증상**: 홈의 꿈 입력창은 **자유 텍스트**인데(`"돼지가 나타나서 저를 쫓아왔어요..."`), `haemong_start`·`lucky_view`가 그 원문 50자를 `query`로 GA4에 보내고 있었다.
**왜 문제인가**: 사용자는 여기에 이름·가족관계·건강 상태를 쓴다. ① Google은 PII 전송을 약관으로 금지(계정 조치 대상) ② 우리 방침의 *"개인을 식별할 수 있는 정보는 수집하지 않습니다"* 와 모순 ③ 같은 방침의 *"꿈 내용은 서버에 저장되지 않습니다"* 와도 모순(GA는 제3자 서버).
**처리**: 원문 전송 제거 → 길이 구간(short/mid/long)과 매칭 여부만 전송. 검색어(`dream_search`)는 콘텐츠 기획의 입력이라 유지하되 **20자 초과·숫자 4자리 이상·이메일·사람 지칭 어미**는 query를 빼도록 필터. 실측으로 정상 키워드는 수집되고 개인정보 의심 입력 6종은 전부 차단되는 것 확인.
**교훈**: **자유 입력을 분석 도구에 그대로 보내지 않는다.** 지금 쓰지도 않는 값을 습관적으로 붙이면 그게 그대로 리스크가 된다.

### [해결] 꿈사주를 내면서 방침·약관을 갱신하지 않았다
**증상**: 생년월일을 localStorage에 저장하는데 개인정보처리방침에 **언급 0건**, 약관 제2조 서비스 내용에도 꿈사주가 없었다.
**처리**: 방침에 생년월일 항목·보관파기·미성년자 조항 반영, 보장 못 할 진술 삭제, 약관에 꿈사주와 면책 추가. 방침에서 약속한 **[저장된 정보 지우기] 버튼을 실제로 구현**(약속만 하고 기능이 없으면 그것도 허위 고지다).
**교훈**: **기능을 내면 법적 문서도 같은 배포에 포함한다.** 다음 기능 출시 시 체크리스트에 넣을 것.

### [해결] Gmail 앱 비밀번호가 가변 태그 서드파티 액션으로 넘어가고 있었다
**증상**: `dawidd6/action-send-mail@v3` — 태그는 언제든 다른 커밋을 가리키게 바뀔 수 있고, 액션 저장소가 침해되면 시크릿이 그대로 넘어간다.
**처리**: 커밋 SHA `4226df7`로 고정 + `permissions: {}` 명시(이 워크플로는 레포 쓰기 불필요).
**교훈**: **시크릿을 넘기는 서드파티 액션은 반드시 SHA로 고정한다.**

### [해결] "사용자 급락" 오인 — 실은 유입원 추적 손실 (09-03)
**증상**: 대규모 수정 다음 날 활성/신규 사용자가 급감한 것처럼 보였다.
**1차 확인(오인의 원인)**: 조회 시각이 **KST 07:46**이었다. 당일 데이터는 하루가 시작도 안 한 상태 + GA4 처리 지연. 완료된 날로 보면 9/2 = 41명으로 14일 범위(36~62) 안이었다. **미완료된 당일 수치로 판단하면 안 된다.**
**2차 확인(진짜 변화)**: 유입원 구성이 바뀌었다. `(not set)` 0 → 25(36%), 네이버 referral 47 → 32. 그런데 **총 세션은 49 → 69로 오히려 늘었다.** 즉 네이버 사용자가 사라진 게 아니라 `네이버 → 구도메인 → 308 → suksuki.com` 체인에서 referrer가 유실돼 미분류된 것.
**처리**: 네이버 서치어드바이저에서 suksuki.com **소유확인이 실패**하고 있었다 — 도메인 이전 후 네이버가 새 인증코드(`67d356f6…`)를 발급했는데 페이지엔 구 도메인 코드만 있었다. 새 코드를 149페이지에 추가(구 코드도 유지 — 리다이렉트 신호 전달에 필요) → 소유확인 통과 → 사이트맵 제출 + 홈 수집 요청.
**교훈**: **당일 데이터로 추세를 판단하지 않는다.** 그리고 도메인을 옮기면 검색엔진 소유확인은 새로 받아야 한다 — 이전 작업 체크리스트에 넣을 것.

### [해결] 미사용 서비스 2곳이 같은 AdSense 게시자 계정을 쓰고 있었다 (09-03)
**발견 경위**: 네이버 서치어드바이저 사이트 목록에서 `sajulucky.vercel.app`·`tarot-hazel-three.vercel.app`을 보고 점검.
**증상**: 두 곳 모두 꿈해몽과 **동일한 게시자 계정 `pub-2084458267795230`** 으로 광고를 싣고 `ads.txt`까지 두고 있었다. 특히 tarot은 **실질 1페이지 사이트에 광고 유닛 3개** — "저가치 콘텐츠 + 광고 과다" 패턴 그 자체.
**왜 문제인가**: AdSense는 사이트가 아니라 **게시자 계정 단위로 심사**한다. 방치된 저품질 사이트가 꿈해몽 재심사에 그대로 불리하게 작용할 수 있었다.
**처리(삭제 아닌 비활성화)**: 광고 로더·유닛 제거, `ads.txt` 삭제(계정 선언 해제), 전 페이지 `noindex,nofollow`, `robots.txt` → `Disallow: /`. 두 저장소 모두 커밋 1개로 되돌릴 수 있게 남김. 각 저장소의 **기존 미커밋 작업물(api/gemini.js 등)은 건드리지 않고** 변경 파일만 명시해 커밋.
**AdSense 콘솔 확인 결과(09-03) — 가설이 한 번 더 확인됨**: 사이트 3개가 **전부 "가치가 별로 없는 콘텐츠"로 반려** 상태였다.
  dreams-come-true-ten(5/7) · tarot(4/18) · sajulucky(4/27). 서로 다른 세 콘텐츠(꿈해몽 142편, 사주 54편, 타로 1편)가
  같은 사유로 반려됐고 **공통점은 `*.vercel.app` 도메인뿐**이다. 콘텐츠 양의 문제가 아니었다는 강한 증거.
**처리 완료**: 운영자가 콘솔에서 **3개 전부 삭제** + `seansaiid@gmail.com`을 계정 사용자로 추가(09-03).
  ※ AdSense는 1인 1계정이라 새 가입은 정책 위반 — 기존 계정에 사용자 추가로 관리 권한만 확보한 것.
**교훈**: 게시자 계정은 공유 자산이다. **한 사이트의 심사를 준비할 땐 같은 계정을 쓰는 다른 사이트도 함께 점검한다.**

### [해결] 에이전트 보고서 7건이 레포 밖 임시 폴더에만 있었다 (09-03)
- **증상**: 운영자가 "사주 버튼이 너무 아래 아니냐"고 지적. 측정해 보니 **9/2 UI/UX 에이전트가 이미 P1으로 지적한 내용**이었다.
- **원인**: ① 보고서가 세션 스크래치 폴더에만 존재(세션 종료 시 소멸) ② P0만 배포하고 P1은 대화에만 남아 증발 ③ 각 지적의 종결 여부를 확인하는 절차 없음
- **파급**: 다음 날 사주 CTA를 **이미 문제 구간으로 판정된 87% 지점**에 새로 넣었다. 재방문율을 고치려 만든 기능이 사용자 87%에게 도달하지 못한다(`scroll` 90% 도달률 약 13%).
- **처리**: 보고서 7건을 `docs/agent-reports/`에 보존. `docs/UIUX_REVIEW.md` 추적표 신설(14개 항목 코드 실측으로 이행 여부 확정). `DECISIONS.md`에 종결 규칙 5조 신설.
- **교훈**: **에이전트를 잘 돌리는 것과 그 산출물을 끝까지 처리하는 것은 별개 문제다.** 분석량이 많을수록 종결 추적이 없으면 그대로 샌다.

### [확인] 발행 큐 읽는 법 — 13건 전부 `approved:true`이지만 대기분은 9건 (09-04, 컨텐츠창)
- **증상이 될 뻔한 것**: `queue.json`을 열면 13건이 **전부 `approved:true`**라 "검수 대기 0, 전부 승인됨"으로 오독하기 쉽다. 나도 처음 집계에서 그렇게 셌다.
- **실제 구조**: 발행 여부는 `approved`가 아니라 **`published` 필드**로 갈린다. `scripts/publish-queue.mjs:11` = `queue.find(e => e.approved === true && !e.published)`. 발행기는 `published:true` + `publishedAt`을 **덧붙일 뿐 항목을 지우지 않는다**(`:72`).
- **실측(09-04)**: 발행완료 4건(washing-machine-overflow, kakao-message-unread, wifi-keeps-disconnecting, subway-gate-not-opening) + **미발행 대기 9건** + 초안 0건. STATUS.md 자동 생성값("검수완료 9건 / 초안 0건")과 일치.
- 확인 명령:
  ```
  python3 -c "import json;q=json.load(open('content-queue/queue.json'));print(len([e for e in q if e.get('approved') and not e.get('published')]))"
  ```
- 대기 9건 카테고리: blocked 5 / body 2 / loss 2. 주3회 기준 **3주치**(규칙 2주치=6건 대비 여유 3건).
- **교훈**: 큐 잔량은 `approved` 개수가 아니라 `approved && !published`로 센다.

### [확인] 콘텐츠 카테고리 편중 + 중복 검사 수단 부재 (09-04, 컨텐츠창)
- **카테고리 편중이 대기분 발행으로 더 심해진다.** `kw-cat-map.csv` 143행 실측: blocked **46/143 = 32.2%** → 대기 9건(blocked 5) 반영 후 **51/152 = 33.5%**. body는 6 → 8로 여전히 최소.
  현재값 blocked 46 · money 22 · people 20 · loss 14 · nature 12 · animal 12 · change 11 · body 6.
- **중복 검사 스크립트가 레포에 없다.** `scripts/` 전수 grep(`중복|duplicate`) 결과 **해당 파일 없음**. `docs/handoff/CONTENT.md` §2가 적어둔 품질 게이트(기존 142편과 중복 문장 0 · 초안 간 중복 ≤3% 등)는 **스크립트 없이 수행된 것으로 보인다**(수행 방식은 확인하지 못했다).
  → 다음 초안 생산 전에 **검사 수단을 먼저 만든다.** 없으면 게이트를 실측으로 통과시킬 방법이 없다.
- **미결정**: 다음 초안 8편의 소재 축(① 카테고리 균형 ② 상위 유입 패턴 복제 ③ 꿈사주 연계). 운영자가 선택을 **보류**했다. `OPERATIONS.md §2-A`의 "일상 소재 롱테일이 옳은 방향" 가설이 아직 미검증(판정 +4주)이라 이 창에서 임의로 방향을 바꾸지 않는다.

### [확인] GA4 설정 감사 — 도메인 전환 후 "다시 연결해야 하나?" (09-04, Admin API)
- 운영자 질문: 도메인을 바꿨으니 GA 연결을 새로 해야 하는 것 아닌가, 수치가 이상하다.
- **수집은 재연결 불필요·정상** — 실브라우저에서 `g/collect?tid=G-MCNS7P3XVT&dl=https://suksuki.com/...&en=page_view` 패킷 직접 포착(2회). 측정ID는 전 페이지 300곳 단일. 수치 하락의 원인은 GA가 아니라 네이버 미노출(별도 실측).
- 운영자가 GCP에서 Analytics Admin API 활성화 → `scripts/ga/ga-admin.mjs`로 설정 조회:
  | 항목 | 값 | 판단 |
  |---|---|---|
  | 시간대 | **Asia/Seoul** | ✅ 시간대별 비교 분석 전부 유효 |
  | 측정ID | G-MCNS7P3XVT | ✅ 사이트와 일치 |
  | **스트림 기본 URL** | **`https://dreams-come-true-ten.vercel.app`** (구 도메인) | ⚠️ 수집 무관, Search Console 연동에 영향 → **UI에서 suksuki.com으로 갱신 필요** |
  | 향상된 측정 사이트검색 파라미터 | `q,s,search,query,keyword` | ⚠️ **우리 CTA 착지 `?s=<slug>`가 검색으로 오인** — 30일 `view_search_results` 5건 중 4건이 `bag-lost`·`parked-car-lost`(슬러그), 1건은 `../../etc/passwd`(봇 프로빙, W1-5 화이트리스트로 클라이언트에서 거부됨) → **`s` 제거 권고** |
  | 데이터 보관 | **2개월** | ⚠️ 탐색(Explore)·퍼널 분석용 이벤트 데이터가 2개월 후 소멸. 전환 전 기준선(8월)이 곧 사라짐 → **14개월로 변경 권고**(무료). 구글 공식: "보관 기간을 늘리면 이미 수집한 데이터 및 아직 삭제하지 않은 데이터에도 적용" — 9/4 내가 "소급 안 됨"이라 잘못 말함, 정정 |
  | Search Console 링크 | Admin API에 엔드포인트 없음(404) | **API로 확인 불가 → UI 확인** |
- **9/4 당일 운영자가 4건 전부 조치 완료. API로 재확인**: 스트림 URL → `https://suksuki.com` · 사이트검색 파라미터 `s` 제거(`q,search,query,keyword`) · 데이터 보관 → **FOURTEEN_MONTHS**(8월 기준선 보존) · Search Console 링크 생성(suksuki.com URL프리픽스 ↔ 웹 스트림).
  남은 것: 라이브러리에서 Search Console 보고서 모음 **게시**(기본 미게시), 데이터는 48시간 뒤부터. `searchConsoleLinks`는 Admin API에 엔드포인트가 없어(404) UI로만 확인 가능.
- **UI 경로(2026-09-04 운영자 화면 캡처로 확정)**: ①스트림 URL·②사이트검색 `s` 제거 = 관리 → **데이터 수집 및 수정 → 데이터 스트림** · ③보관 14개월 = 관리 → **데이터 수집 및 수정 → 데이터 보관** · ④Search Console = 관리 → **제품 링크**(사이드바 하단) → Search Console 링크.
  내가 공식 문서를 근거로 안내한 "데이터 설정 → 데이터 보관"은 **이 화면에 없는 경로였다**. 문서의 메뉴명이 실제 UI와 어긋날 수 있으므로, 경로 안내는 운영자 화면을 먼저 확인하는 편이 빠르다.

### [해결] 스크롤 깊이 계측 추가 — 배포 후 실브라우저 검증에서 0건, rAF 버그 발견·수정 (09-04)
- **목적**: 꿈사주 인라인 모듈을 본문 51% 지점에 둘지 결정하려면 도달률이 필요한데 GA4 기본 `scroll`은 90%에서만 발화.
  운영자 승인 후 `scroll_25/50/75` 이벤트를 143 dream 페이지 + 템플릿에 추가(`35f4fb3`).
- **증상**: 배포 직후 실브라우저(375x812) 검증에서 scrollY 724→7236까지 움직였는데 **이벤트 0건**.
- **원인**: 브라우저 패널 탭이 `visibilityState=hidden` → `requestAnimationFrame`이 실행되지 않음(500ms 대기 미실행 확인).
  스로틀이 `sdT = requestAnimationFrame(...)`으로 pending 고정 → 이후 모든 scroll이 `sdT!==null` 가드에 막힘.
  실사용에서도 사용자가 앱/탭 전환 후 돌아오는 경로에서 동일 발생 가능한 결함.
- **처리**: 스로틀을 `setTimeout(sdCheck,100)`으로 교체(`a37759d`, 144파일 1줄, diff -144/+144).
  재검증: 30%→scroll_25, 55%→scroll_50, 80%→scroll_75, 100%·재스크롤 중복 0, param `page` 정상.
- **교훈**: **코드 존재(grep) ≠ 동작.** 계측은 배포 후 실브라우저에서 발화까지 확인한다. hidden 탭 조건을 테스트에 포함한다.
  rAF는 hidden에서 멈추므로 "반드시 실행돼야 하는" 지연에는 쓰지 않는다.
- **판정 규칙**: 도달률은 **완성 데이터 7일 이상** 모인 뒤에만 본다(DECISIONS.md 당일 데이터 금지). 기준: `scroll_50` ÷ `page_view`.

### [진행중] 꿈사주 문안 2차 초안 50블록 — 검수 대기 (09-04)
- `docs/drafts/saju-content-v2-draft.md`. 꿈 처방 40 + 오늘의 흐름 10. 운영자 검수 후 반영(미반영 상태).
- 1차본에서 글자 수를 손으로 세어 적었더니 **49/50 오기**. `scripts/measure-draft.py`로 실측·기록·범위·용어 검사하도록 바꿈. 숫자는 사람이 적지 않는다.
- **기계 검사는 전부 통과**(09-04 재실행 `python3 scripts/measure-draft.py docs/drafts/saju-content-v2-draft.md`, exit 0):
  `블록 50개 · 평균 140자 / 범위 밖 0개 / 사주 용어 없음`.
  → 남은 것은 **운영자의 내용 검수뿐**이다. 수정 의견이 없으면 컨텐츠창에서 더 진행할 수 없다(반영은 개발창 DEV.md §5-1).

### [미해결] 예약 작업 실행 자체가 재현 안 됨 — 원인 파악 도구 한계 (09-04 11:37)
- 11:00 정기 실행이 산출물 없이 끝난 것을 확인한 뒤, **11:27 즉시 발화로 재현 테스트**를 했다.
- 결과: **10분 넘게 경과해도 `lastRunAt`이 갱신되지 않았다.** 로컬·원격 커밋 전무.
  즉 이번엔 dispatch(작업 시작) 자체가 안 됐거나, 시작됐어도 상태가 전혀 기록되지 않는 상태다.
- 정기 실행(11:03)은 `lastRunAt`이라도 찍혔는데 이번 즉시발화는 그것조차 없다 — 두 실패의 양상이 다르다.
- **가진 도구로는 이 이상 원인 규명이 불가능하다.** 예약 작업의 실행 로그·에러를 조회하는 도구가 없다.
  세션 간 메시지로 관련 세션에 문의했으나 응답 없음.
- **조치**: 일일 cron은 원상 복구해뒀다(이후 운영자 지시로 09:00으로 재조정, 2026-09-04). 다만 오늘 두 번 연속(정기+즉시발화) 결과를 못 냈으므로
  **내일도 자동 실행을 신뢰하지 않는다.** 자동 보고가 안 오면 그 즉시 수동으로 직접 돌린다.
- **운영자 확인 요청**: 앱 사이드바의 "Scheduled" 섹션에서 이 작업의 상태·오류 표시가 보이는지 확인 부탁.
  API로는 안 보이는 정보가 UI에는 있을 수 있다.

### [사고] 09-04 11:00 예약 추적이 결과를 내지 못함 + 스크립트 버그 발견 (09-04 11:19)
- **증상**: `dreams-naver-recovery-daily`(매일 11:00) `lastRunAt`은 11:03 KST로 기록됐으나
  커밋·`recovery-log.json`·WORKLOG 어디에도 그 시각의 산출물이 없다. 원인은 확인 못함(다른 세션 확인 요청, 응답 없음).
  → 운영자에게는 자동 보고가 안 온 것으로 보였다. 수동으로 즉시 재실행해 대응.
- **별개로 스크립트 자체 버그 발견**: `naver-recovery-check.mjs`의 `settled` 계산이 `.slice(0,-1)`로
  **오늘뿐 아니라 어제까지 판정 대상에서 제외**하고 있었다. 그 결과 "전환 후 완성 데이터" 평균이
  9/2(41명) 하나만 반영해 **-16%로 실제보다 덜 심각하게** 나왔다.
  GA4 처리 지연은 당일 몇 시간 내에서 끝나므로 다음날 아침이면 전날 데이터는 완성됐다고 봐도 된다 —
  시간대별 대조로 이미 확인된 사실인데 스크립트에는 반영이 안 돼 있었다.
- **수정 후 재실행한 정확한 수치**: 전환 후 완성 데이터 **2일(9/2, 9/3) 평균 사용자 30.5명,
  기준선(48.7명) 대비 -37%.** 네이버 노출은 아침과 동일(22/50, 변화 없음).
- **조치**: `.slice(0,-1)` 제거, 코멘트 문구 수정. 예약 작업 결과 미도달 건은 원인 불명으로 남김 —
  다음 실행(9/5)에서 같은 현상이 재발하면 예약 작업 자체를 점검한다.

### [확정] 09-03 완성치 — 기준선 대비 -55%, 09-04 아침도 심각 (09-04 실측)
- 운영자가 "오늘 활성/신규 사용자가 1이다. 평소 이 시간이면 15~20인데 심각하다"고 지적(09-04 08:35).
- **검증**: 같은 요일 0~5시(완전 처리된 시간대) 비교로 처리 지연 가능성부터 배제.
  09-04 1명 · 09-03(목) 2명 · 08-28(금, 1주전) 7명. 실시간 API로도 활성 사용자 2명 확인 →
  **사이트·계측 정상, 유입 자체가 없는 것.**
- **9/3 완성 데이터**: 사용자 20명 (기준선 48.7명 대비 **-59%**). 09-03 저녁 추정치보다 나쁘다.
- **내 변경이 원인인지 대조**: 노출 페이지(parked-car-lost)와 미노출 페이지(bag-lost)가 같은 시각(18:03)
  같은 커밋으로 수정됨, canonical도 둘 다 정상 → **기술적 차이 없음. 09-03 대량 수정이 원인이라는 근거 없음.**
- **네이버 노출**: 19/50(09-03 저녁) → 22/50(09-04 아침). house-remodeling·도와주는 꿈이 신규 노출
  (둘 다 제출한 URL) — 단 하루치라 제출 효과로 단정 불가. **1위 유입원 bag-lost(기준선 147세션)는
  12시간 경과 후에도 여전히 미노출.**
- **지금 추가로 할 수 있는 기술적 조치 없음.** URL 30개 전량 제출 완료, 사이트·계측 정상 확인됨.
  남은 것은 매일 실측 추적뿐. `dreams-naver-recovery-daily`(2026-09-04부터 매일 09:00)가 계속 확인.

### [진행중] 네이버 수집 요청 제출 — 30개 URL (09-03)
- 도메인 전환 후 미노출로 확인된 페이지에 대해 네이버 서치어드바이저 웹 페이지 수집 요청 제출
- 09-03 21:32~21:33: 상위 5개(bag-lost·phone-lost·shoes-lost·receiving-money·house-remodeling) 운영자 직접 제출
- 09-03 이후: `scripts/recovery-submit-priority.txt`의 나머지 25개(A 미노출 20 + B 구도메인만 5) 운영자 전량 제출 완료
- **총 30개 제출.** 이미 신 도메인 노출 중이던 19개는 제외(불필요)
- 효과 판정은 `dreams-naver-recovery-daily`(매일 11:00 예약 작업)가 네이버 노출 재조회 + GA4 완성 데이터로 매일 확인.
  판정 기준: 전환 후 완성 데이터 7일 평균 사용자가 기준선(48.7명) 회복
- 수집 요청 제출 후 실제 반영까지 걸리는 기간은 확인하지 못함 — 매일 추적으로 실측할 것

### [오진 정정] "사용자 급락" 2건 — 둘 다 GA4 처리 지연이었다 (09-03)
- **증상**: 운영자가 일일 활성/신규 사용자 감소를 지적. 도메인 전환(9/2)으로 색인이 깨진 것 아닌지 우려.
- **1차 오진(9/2)**: `(not set)` 25건을 보고 "리다이렉트가 유입원을 잃고 있다"고 결론.
  → 다음날 같은 날짜를 다시 조회하니 **`네이버 33, (not set) 0`**. 처리가 끝나며 제자리를 찾았다.
- **2차 오진(9/3)**: "같은 시각(0~19시) 누적 18명 vs 이전 9일 평균 48.8명 = **-63% 급락**"으로 결론.
  → **틀렸다.** 오늘분이 미처리라 그 비교 자체가 성립하지 않았다.
- **실제 확인 방법과 결과**:
  - 실시간 API(`runRealtimeReport`) — 처리 지연 없음 → **활성 사용자 2명, 꿈 페이지 조회 중**. 사이트 정상
  - 전 페이지 200 + GA 태그 정상
  - 시간대별로 **13시 이후 전부 0** = 미처리 구간. 그날 첫 HTML 배포는 **18:03**이라 배포 탓도 아니다
  - **7일 평균 47.9 vs 이전 7일 48.9 (-2%)** — 변화 없음
  - 네이버 유입(세션): 8/28 34 · 8/29 65 · 8/30 55 · 8/31 46 · 9/1 44 · **9/2 33**(전환일) → 전환 후에도 정상 유입
- **처리**: `DECISIONS.md`에 "GA4 당일 데이터로 판단하지 않는다" 4조 신설(검증 순서 포함).
  `OPERATIONS.md` §3-A 일일 관측 지침(기준선 40~65명, 7일 평균 48명) 신설.
  예약 작업 `dreams-traffic-verify-0903`(9/5 11:00)으로 **9/3 완성 데이터 재확인**.
- **교훈**: **당일 데이터로는 아무것도 판단할 수 없다.** 이틀 연속 같은 함정에 빠졌다.
  급락이 의심되면 추론하지 말고 **실시간 API부터** 본다 — 처리 지연을 받지 않는 유일한 지표다.
- **미확정**: 도메인 전환이 네이버 순위에 주는 영향은 **2~3주는 지나야** 드러난다. 계속 관측 대상.

### [추적] 네이버 재색인 회복 일일 실측 — 09-04 3회차(수동), 예약 작업은 또 발화 기록 없음
- **예약 실패 확인 방법**: `list_scheduled_tasks` 조회 결과 `dreams-naver-recovery-daily`에 **`lastRunAt` 필드 자체가 없다**(같은 목록의 `memimo-auth-logout-verify`에는 존재). 09-04 09:03 예정분도 기록 없음 = **3회 연속 미발화**. `SKILL.md`는 정상(문법·경로 이상 없음). **원인은 확인하지 못했다**(실행 로그 조회 수단 없음). 다음 예정 09-05 09:03.
- **수동 실행**: `node scripts/naver-recovery-check.mjs` (09-04 18:50 KST, exit 0). `recovery-log.json` 7건째 누적.
- **GA4 일별(완성 데이터)**: 09-01 48 → **09-02 41**(전환일) → **09-03 22 · 신규 21 · 세션 26**. 09-04는 처리중(사용자 10)이라 판정 제외.
  - 09-03 값은 오전 11:19 실행 시 20 → 18:50 실행 시 **22**로 상향. GA4가 미처리분을 계속 반영 중이므로 09-03도 아직 완결로 보지 않는다(`dreams-traffic-verify-0903`, 09-05 11:00에 재확인).
  - 전환 후 완성 2일 평균 **31.5** = 기준선 48.7 대비 **-35%**. 09-03 단일 22는 기준선 일 범위 **32~67의 하한 아래**.
- **유입원 일별 세션**(`sessionSource` × `date`): 네이버 09-01 47 → 09-02 35 → **09-03 18**. 구글은 08-26~09-04 전 구간 **0**. 직접 유입은 전환 전 0~3 → 09-02 12 · 09-03 8로 증가했으나 네이버 감소분(-29)을 설명하지 못한다.
- ~~**랜딩페이지 폭 붕괴(가장 뚜렷한 실측)**~~ → **09-06 철회.** 전환 전 7일 99개와 하루치 19개를 비교한 **무효 수치였다**(09-06 정정).
   같은 조건(하루)으로 재측정하고 사용자 수로 정규화한 결과, **폭 축소는 사용자 감소에 비례하며 추가 악화가 아니다**:
   | 일자 | 사용자 | 랜딩페이지 | 페이지/사용자 |
   |---|---|---|---|
   | 08-28(금) | 39 | 29 | 0.74 |
   | 08-29(토) | 62 | 38 | 0.61 |
   | 09-02(수) | 41 | 30 | 0.73 |
   | 09-03(목) | 22 | 19 | 0.86 |
   | 09-04(금) | 21 | 16 | 0.76 |
   | 09-05(토) | 19 | 15 | 0.79 |
   페이지/사용자는 전 기간 0.61~0.86으로 동일하다. "롱테일이 죽고 있다"는 해석은 근거가 없다.
- **노출 지표의 한계를 실측으로 확인** — 이 수치를 앞으로 이렇게 읽는다:
  - `naver-recovery-check.mjs:73`은 모바일 SERP HTML에서 `suksuki.com` **문자열 등장 횟수**를 센다. 표의 "노출 13/14/15/28"은 순위도 노출수도 아니다(결과 카드 1건이 도메인 문자열을 여러 번 포함). **`>0`인지 아닌지만 유효**하고, 리포트의 22/50은 그 이진값이라 그대로 유효하다.
  - 노출 여부 × 09-03 세션 교차표: 노출O·세션O 8 / **노출O·세션X 14** / **노출X·세션O 4** / 노출X·세션X 24 (n=50). 피어슨 r=0.076이나 **09-03 총 세션이 26뿐이라 상관계수는 판정 근거가 못 된다**.
  - 확정적인 건 **"미노출인데 유입 발생" 4건**(가방을 잃어버리는 꿈 6세션, 친구가 죽는 꿈·금 줍는 꿈·아기꿈 각 1). 추적 검색어가 페이지 제목에서 뽑은 **추정치**라는 기존 한계와 일치한다. → 네이버 서치어드바이저 「리포트 → 콘텐츠 노출/클릭」의 실제 질의를 받아 `recovery-targets.json`을 교체해야 지표가 정확해진다(운영자 몫, 대기 중).
- **네이버 노출 3회 연속 동일**: 09-04 08:38 · 11:19 · 18:50 모두 노출 22 / 미노출 26 / 구도메인만 2 / 손실세션 656. 하루 안에서는 변화 없음(하루 단위로 회복 추세를 판정하지는 않는다).
- **판정: 미회복.** 추가로 취할 기술 조치는 찾지 못했다. 수집 요청·sitemap·소유확인은 09-03에 제출 완료 상태.

### [미해결·보류] 항목
- **🔴 UI/UX 미이행 7건** — `docs/UIUX_REVIEW.md` 참조. 그중:
  - **8-1 푸터 대비 1.49:1 (84개 파일 + 템플릿)** — 개인정보처리방침 링크가 사실상 안 보인다. **AdSense 심사 실제 확인 항목** → 심사 전 처리 후보
  - 8-3 자기 자신을 가리키는 링크 30개 파일 — "관리되지 않는 사이트" 신호
  - **4a·4b·4c + 사주 CTA 위치** — 운영자 지시(09-03): **AdSense 승인 후, 데이터를 보고 한 묶음으로 재설계.** 개별 수정 금지
  - 5 오늘의 꿈 위젯 상세 페이지 확산 — 4번과 같은 지면이라 함께 판단
- **AdSense 승인** — 과거 "저가치 콘텐츠"로 반려된 이력. 기술적 원인은 제거했으나 **구글 색인이 쌓여야 재신청 가능**. 승인돼도 현 트래픽에선 월 수천 원 수준이 현실적 기대치
- ~~**초안 8편 검수** — 사용자 차례~~ → 09-04 큐 실측으로 **전건 승인 확인**, 미발행 9편은 자동 발행 대기 (미해결 아님)
- **젊은 층 공략 여부** — GA에 연령 데이터가 없다(Google 신호 미설정). 간접 증거(상위 페이지가 주차·리모델링 등 생활 소재, 네이버 97%)로는 **30~50대로 추정**. 신규 획득이 필요한 전략이라 지금은 보류
- **11ty 이관** — 발행 자동화로 실질 고통이 줄어 우선순위 하향
- **Vercel Hobby 상업 이용 회색지대** — AdSense 승인 후 문제되면 Cloudflare Pages로 이전(0원)

---

## 🧰 자주 쓰는 명령

```bash
node saju/verify.js              # 만세력·음력 검증 319건
node scripts/verify-publish.mjs  # 발행 무결성 검증
node scripts/health-check.mjs    # 사이트 상태 점검
node scripts/fetch-ga4.mjs 30    # GA4 최근 30일 수집
python3 scripts/build-dreams-db.py     # CTA 착지 DB 재생성
python3 scripts/build-dream-index.py   # 꿈사주 선택기 인덱스
node scripts/build-saju-bundle.js      # 브라우저 번들
```
